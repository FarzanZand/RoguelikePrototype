using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ShowLeo : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("HI new branch!"); 
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
